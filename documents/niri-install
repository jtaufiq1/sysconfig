NIRI_REPO=
TARGET_DIR=
RES_DIR=
BIN_DIR=

# [RECOMMENDS]
# alacritty, fuzzel, waybar, mako-notifier, xdg-desktop-portal-{gtk,gnome}, gnome-keyring
# swaybg, swayidle, swaylock
# xwayland-satellite

# Create and add systemd service for swaybg & swayidle.
# Add to graphical-want (user) and manage by systemd

# Configure waybar & mako: add service to niri
# Install applets: pavucontrol, pasystray, blueman-applet

# Binary
cp $TARGET_DIR/niri $BIN_DIR
cp $TARGET_DIR/niri-session $BIN_DIR

# Resources
## destop, portals, systemd service & target
cp $RES_DIR/niri.desktop $RES_DEST
cp $RES_DIR/niri-portals.conf $RES_DEST
cp $RES_DIR/niri.service RES_DEST
cp $RES_DIR/niri-shutdown-target RES_DEST


#sudo cp target/release/niri /usr/local/bin/
#sudo cp niri-session /usr/local/bin/

#sudo cp niri.service niri-shutdown.target /etc/systemd/user
#sudo cp niri.desktop /usr/local/share/wayland-sessions/

#sudo cp niri-portals.conf /usr/local/share/xdg-desktop-portal/

#sudo cp cosmic-ext-niri.desktop /usr/share/wayland-sessions/
